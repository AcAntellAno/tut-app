- Components are the bulding blocks of React
    - Each component will contain its own template and logic

State -> the data used in a component in that point in time

useState => this is a hook. Hook is a special type of func that does a certain job.
The useState hook gives us a way to use a reactive value and change it when we want.